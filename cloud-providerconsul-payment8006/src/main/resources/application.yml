server:
  port: 8006
spring:
  application:
    name: consul-provider-payment

# consul注册中心配置信息
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        #hostname: 127.0.0.1
       server-name: ${spring.application.name}
